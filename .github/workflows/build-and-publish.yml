name: Build and Publish to github

on:
  # Manually triggered workflow using the "Run workflow" button
  workflow_dispatch:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '8'

      - name: Build with Maven
        run: mvn -B package -DskipTests

  publish:
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 8 & Set settings.xml
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '8'
          # This creates a temporary settings.xml mapped to the 'thrust-devs-orderexpansion-repo' server ID
          server-id: thrust-devs-orderexpansion-repo
          settings-path: ${{ github.workspace }}

      - name: Publish
        run: mvn deploy -DskipTests --batch-mode -s ${{ github.workspace }}/settings.xml
        env:
          GITHUB_USERNAME: ${{ secrets.ORG_PUBLISH_USERNAME }}
          GITHUB_TOKEN: ${{ secrets.ORG_PUBLISH_TOKEN }}
